# ModernBERT-JP-1B settings

Configuration-only settings aligned to `sbintuitions/sarashina2.2-1b` for MIREI experiments. Model initialization should load the tokenizer from `sbintuitions/sarashina2.2-1b` via `tokenizer_name_or_path`.
